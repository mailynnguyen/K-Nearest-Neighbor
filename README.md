# K-Nearest-Neighbor (KNN)

### To Run:

1. Clone the repo in your terminal
2. Once in the correct project directory, run ``python main.py``

### Summary:

In this project, I build a K Nearest Neighbor algorithm. This algorithm can take in a large or small dataset and find the most effective set of features to classify a new "object/item". I am using the nearest neighbor classification.
